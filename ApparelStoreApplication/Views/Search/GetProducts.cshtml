@using ApparelStoreWebService.Models.DB;
@model ProductViewModel
@{
    ViewData["Title"] = "GetProducts";
}
@{
    List<ProductViewModel> Model = (List<ProductViewModel>)ViewBag.product;
}

@*<div class="row">
        <input type="submit" value="Submit"  class="btn btn-success"/>
    </div>*@
<div class="container">
    <div class="row">
        <div class="col-md-12">
            <input type="submit" value="VIEW CART" class="btn-dark" onclick="@("window.location.href='" + @Url.Action("ViewCart", "Cart") + "'")" />

        </div>
    </div>
</div>
<div class="album py-5 bg-light">
    <div class="col-md-12">
        <div class="row">
            @*<div class="col-md-5">*@
            @for (int i = 0; i < Model.Count; i++)
            {
                <form asp-action="AddToCart" asp-controller="Cart" method="post">
                    <div class="col-md-4">
                        <div class="card mb-4 shadow-sm">
                            <div class="col-md-2 column productbox">
                                <img src="@Model[i].ProductImage" alt="Card image cap" width="200" height="200"><input type="hidden" name="ProductImage" value="@Model[i].ProductImage" /> class="img-responsive">
                                <div class="card-body">
                                    <p>
                                        <tr>
                                            <label>@Model[i].Title</label>
                                            <input type="hidden" name="Title" value="@Model[i].Title" />
                                        </tr>
                                    </p>
                                    <p>
                                        <tr>
                                            <label>@Model[i].Price</label>
                                            <input type="hidden" name="Price" value="@Model[i].Price" />
                                        </tr>
                                    </p>
                                </div>
                            </div>
                        </div>
                    </div>
                    </form>
                    <div class="col-md-2 order-div">
                        <br /><input type="submit" class="btn btn-success" value="AddToCart" /><br />
                    </div>
            //</div>
            
            

            @*<div class="col-md-4">

            <div class="card mb-4 shadow-sm">
                <img class="card-img-top" src="@Model[i].ProductImage" alt="Card image cap" width="200" height="200"><input type="hidden" name="ProductImage" value="@Model[i].ProductImage" />


                <div class="card-body">
                    <p class="card-text">
                        <tr>
                            <label>@Model[i].Title</label>
                            <input type="hidden" name="Title" value="@Model[i].Title" />
                        </tr>
                    </p>
                    <p>
                        <tr>
                            <label>@Model[i].Price</label>
                            <input type="hidden" name="Price" value="@Model[i].Price" />
                        </tr>
                    </p>*@
            @*<p>
                <tr>
                    <label>@Model[i].Description</label>
                    <input type="hidden" name="Description" value="@Model[i].Description" />
                </tr>
            </p>*@
        //</div>
                            @*<div class="col-md-2 order-div">
                                <br /><input type="submit" class="btn btn-success" value="AddToCart" /><br />
                            </div>
                        </div>*@
                    //</div>

                    <input type="hidden" name="ProductId" value="@Model[i].ProductId" />

                    <input type="hidden" name="Title" value="@Model[i].CategoryId" />
                    <input type="hidden" name="Title" value="@Model[i].SubCategoryId" />
                    <div class="d-flex justify-content-between align-items-center"></div>

//</form>
                }

                <div>
                </div>
            </div>
            <div class="col-md-4"></div>
        @*</div>

    </div>*@


